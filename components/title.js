

export default function Title({name}) {
    return (
      <>
        <h1 style={{fontFamily: 'Hubballi', fontSize: 35, transform: 'scale(1, 1.3)', textAlign: 'center'}}>{name}</h1>
        <hr style={{width: 300}} />
      </>
    );
  };